// Fill out your copyright notice in the Description page of Project Settings.


#include "../../Public/UI/PauseMenu.h"

/*FReply UPauseMenu::NativeOnFocusReceived(const FGeometry & InGeometry, const FFocusEvent & InFocusEvent) {
  ResumeButton->SetUserFocus(GetOwningPlayer());
  //Nos cercioramos que lo hemos creado con el PlayerController correspondiente, normalmente como lo
  //hacemos nosotros desde su HUD
  return FReply::Handled();
  //Si colocamos Unhandled haremos que el sistema pueda recoger el foco en casos como SelfHitTestInvisible
  //en widgets hijos de este.
}

void UPauseMenu::SynchronizeProperties() {
  Super::SynchronizeProperties();

  //Banner->SetText(BannerText);

}*/